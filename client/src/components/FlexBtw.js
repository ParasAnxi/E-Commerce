import styled from "@emotion/styled";
import { Box } from "@mui/material";

const FlexBtw = styled(Box)({
    display:'flex',
    alignItems:'center',
    justifyContent:'space-between',
});

export default FlexBtw;